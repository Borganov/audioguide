Create a test_data directory and run the following commands to export your database to a database agnostic json file:
python manage.py dumpdata > test_data/test_file_1.json

At any point if you want to restore the database to that point simply run:
python manage.py loaddata test_data/test_file_1.json
